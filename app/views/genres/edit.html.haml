= render 'form'
.text-center.edit-links
  = link_to "Delete genre", genre_path(@genre), method: :delete, data: { confirm: "Are you sure you want to delete this genre?" }
  |
  = link_to "cancel", genres_path
